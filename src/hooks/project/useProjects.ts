
// Refactored version that imports from the smaller files
import { useProjects as useProjectsComposed } from "./projects";

export const useProjects = useProjectsComposed;

export default useProjects;
